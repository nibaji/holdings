import React from "react";
import { View } from "react-native";

import Style from "../styles/components/divider.styles";

const Divider = () => {
  return <View style={Style.dividerLine} />;
};

export default Divider;
